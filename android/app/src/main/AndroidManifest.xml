<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.youssef_jaber.casio_fx991">

    <!-- Storage Permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- Media Store Access (API 30+) -->
    <uses-permission android:name="android.permission.MANAGE_MEDIA" />

    <!-- Internet for potential updates -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- FFmpeg Processing -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:label="Youssef Jaber Converter"
        android:icon="@mipmap/ic_launcher"
        android:debuggable="true"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
        </activity>

        <!-- FFmpeg Configuration -->
        <service
            android:name="com.arthenica.ffmpeg.FFmpegService"
            android:enabled="true"
            android:exported="false" />

        <!-- Content Provider for MediaStore -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.youssef_jaber.casio_fx991.provider"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Meta-data for Flutter -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Keep Alive Policy for Background Processing -->
        <service
            android:name=".services.VideoConversionService"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>
